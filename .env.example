# Anthropic API 配置（必需）
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_BASE_URL=https://api.anthropic.com

# 数据存储目录（可选，默认为 ./data）
DATA_DIR=./data

# 服务器端口（可选，默认为 3000）
PORT=3000

# 互联网搜索API配置（可选，用于AI智能搜索）
# 注意：至少配置一个搜索API，AI才能在不确定时搜索互联网

# Tavily API（推荐，专为AI设计）
# 获取API Key: https://tavily.com
TAVILY_API_KEY=your_tavily_api_key_here

# Serper API（备选）
# 获取API Key: https://serper.dev
SERPER_API_KEY=your_serper_api_key_here

# 说明：
# 1. ANTHROPIC_API_KEY 是必需的，用于AI助手功能
# 2. TAVILY_API_KEY 或 SERPER_API_KEY 是可选的，但强烈推荐配置
# 3. 配置搜索API后，AI会在不确定如何完成任务时自动搜索：
#    - 命令市场中的相关脚本
#    - 本地知识库
#    - 互联网上的解决方案
# 4. 如果不配置搜索API，AI仍然可以工作，但只能依赖自身知识
